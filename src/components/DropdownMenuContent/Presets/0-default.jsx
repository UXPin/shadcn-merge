import * as React from 'react';
import DropdownMenuContent from '../DropdownMenuContent';

export default (
  <DropdownMenuContent uxpId='dropdown-menu-content-1' align='start' sideOffset={5}>
    Menu Content
  </DropdownMenuContent>
);